首先确保安装了wget，如果没有则用yum install -y wget进行安装
接着执行以下步骤：
wget http://repo.mysql.com/mysql-community-release-el7-5.noarch.rpm
rpm -ivh mysql-community-release-el7-5.noarch.rpm
yum install -y mysql-server
service mysqld start
安装成功，默认root用户密码为空
